# Bug Fix Plan — Review 2026-06-09

## 📅 Thông Tin
- **Thời gian**: 2026-06-09
- **Workspace**: D:\Quang\kilocode
- **Tổng số lỗi mới phát hiện**: 7

---

## 🐛 DANH SÁCH LỖI

### #77 — Duplicate dead code block trong `excel-import.js` (🟡 MEDIUM) ✅ ĐÃ SỬA
- **File**: `modules/excel-import.js`
- **Fix**: Xóa block duplicate (12 dòng code reset bị copy-paste sai).

### #78 — `useServer` state không reset khi logout/401 (🔴 HIGH) ✅ ĐÃ SỬA
- **File**: `modules/storage.js`, `modules/auth.js`, `modules/api.js`
- **Fix**:
  1. `auth.js:handleLogout()` — thêm `storage.useServer = false`
  2. `api.js:handleAuthError()` — thêm `storage.useServer = false`
  3. `storage.js:init()` — dùng dynamic `import('./api.js')` để tránh circular dependency

### #79 — `useServer` không set false khi localStorage fallback login (🔴 HIGH) ✅ ĐÃ SỬA
- **File**: `modules/auth.js:66`
- **Fix**: Thêm `storage.useServer = false` trong nhánh localStorage fallback login.

### #80 — Course clear filter không reset về "Tất Cả" (🟢 LOW) ❌ CẦN SỬA
- **File**: `index.html:528`, `modules/course.js:141`
- **Nguyên nhân**:
  - `index.html:528`: `<option value="Đang mở" selected>` — mặc định filter theo "Đang mở"
  - `course.js:141`: `clearCourseFilterInputs()` reset status về `'Đang mở'` thay vì `''`
- **Fix**:
  1. `index.html:528`: Bỏ `selected` khỏi `<option value="Đang mở">`
  2. `modules/course.js:141`: Đổi `courseFilterStatus.value = 'Đang mở'` → `courseFilterStatus.value = ''`

### #81 — Payment record mất `id` khi update/create (🔴 HIGH) ❌ CẦN SỬA
- **File**: `modules/payment.js:298-311` và `modules/payment.js:345-352`
- **Nguyên nhân**:
  - `handlePaymentConfirm` (line 298): `record` object thiếu `id` → replace existing record mất id.
  - `handleSavePaymentStatuses` (line 345): new record push thiếu `id`.
- **Fix**: Thêm `generateId` import + `id` field vào cả 2 locations.
  ```js
  // handlePaymentConfirm:
  const record = {
      id: existingIndex !== -1 ? state.paymentRecords[existingIndex].id : generateId('pay_'),
      studentId, courseId, month, status, method, updatedAt
  };
  // handleSavePaymentStatuses:
  state.paymentRecords.push({
      id: generateId('pay_'),
      studentId, courseId, month, status, method, updatedAt
  });
  ```

### #82 — `DELETE /courses/:id` thiếu permission check ✅ FALSE POSITIVE
- **Verification**: Route đã có `checkPermission('courses', 'delete')`. Không cần sửa.

### #83 — `handleAddDate` date validation bypass (🟡 MEDIUM) ❌ CẦN SỬA
- **File**: `modules/attendance.js:39-41`
- **Nguyên nhân**: `if (!date && attendDate)` — nếu element null thì bypass validation.
- **Fix**: Đổi `if (!date && attendDate)` → `if (!date)`

---

## 📋 CÔNG VIỆC THỰC HIỆN (3 fixes còn lại)

| # | Ưu tiên | File | Hành động |
|---|---------|------|-----------|
| 1 | 🔴 HIGH | `modules/payment.js` | Thêm `generateId` import + `id` vào `handlePaymentConfirm` và `handleSavePaymentStatuses` |
| 2 | 🟡 MEDIUM | `modules/attendance.js` | Đổi `!date && attendDate` → `!date` |
| 3 | 🟢 LOW | `index.html` + `modules/course.js` | Bỏ `selected` + reset về `''` |

Sau khi code fix xong → Ghi review notes vào `.kilo/review-notes.md`.
